import styled from 'styled-components';

const Styled = styled.div`
	position: absolute;
	top: 5px;
	right: 10px;
	cursor: pointer;
	span {
		font-size: 24px;
		color: #dc7777;
	}
`;

export default Styled;
