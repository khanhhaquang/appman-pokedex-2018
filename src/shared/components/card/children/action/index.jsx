import React from 'react';
import Styled from './index.styled';

const Action = props => {
	return (
		<Styled className='action'>
			<span className='close'>x</span>
		</Styled>
	);
};

export default Action;
